# Pinned to the exact environment that produced the numbers reported in
# README.md (Python 3.12.8, macOS arm64). Enough to load a checkpoint,
# forecast, and evaluate.
#
#   python -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#   .venv/bin/pip install -e . --no-deps
#
# For looser, forward-compatible bounds use `pip install -e .` instead; the
# code is also known to work on torch 2.14.

torch==2.6.0
numpy==2.2.3
tqdm==4.67.1
xarray==2024.11.0
h5netcdf==1.8.1
h5py==3.16.0
netCDF4==1.7.4
